$background2: #000090;
$background1: white;

html {
	background: radial-gradient(circle, $background1, $background2);
}

body {
	background: radial-gradient(circle, $background1, $background2);
}

div.title {
	color: black;
}
