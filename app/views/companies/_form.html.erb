<%= form_for @company do |f| %>
  	<%= f.error_messages %>
  	<p>
    	<%= f.label :name %><br />
    	<%= f.text_field :name %>
  	</p>
	<p>
		<%= f.label :image_url %><br/>
		<%= f.text_field :image_url %><br/>
	</p>
  	<p><%= f.submit %></p>
<% end %>
