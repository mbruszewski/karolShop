
<div id="container">
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>
	<div id="title">
 	 	<%= content_tag :h1, yield(:title) %>
	</div>
	<br/>
  <%= yield %>
</div>
