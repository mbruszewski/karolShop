1
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button" %>
<% end %>
2
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button2" %>
<% end %>
3
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button3" %>
<% end %>
4
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button4" %>
<% end %>
5
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button5" %>
<% end %>
6
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button6" %>
<% end %>
7
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button7" %>
<% end %>
8
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button8" %>
<% end %>
9
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button9" %>
<% end %>
10
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button10" %>
<% end %>
11
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button11" %>
<% end %>
12
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button12" %>
<% end %>
13
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button13" %>
<% end %>
14
<% for company in @companies %>
  <%= button_to company_name(company), root_path, class: "button14" %>
<% end %>


